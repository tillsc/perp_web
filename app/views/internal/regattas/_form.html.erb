<%= bootstrap_form_for [:internal, @regatta] do |f| %>
  <%= hidden_field_tag :referrer, params[:referrer] %>

  <%= f.text_field :name %>

  <%= f.collection_select :organizer_id, @organizers, :id, :name %>

  <%= f.text_field :year, type: 'number', required: true %>
  <%= f.text_field :from_date, type: 'date', required: true %>
  <%= f.text_field :to_date, type: 'date'  %>

  <%= f.check_box :entry_closed %>

  <%= f.select :show_age_categories, Regatta::AGE_CATEGORIES, include_blank: "Keine anzeigen" %>
  <%= f.check_box :show_countries %>

  <%= f.text_field :currency %>

  <% if @regatta.new_record? %>
    <%= f.select :copy_events_from_regatta_id,
        options_from_collection_for_select(@copy_from_regattas, :id, :name),
        include_blank: "Keine Rennen kopieren", label: "Rennen kopieren von" %>
  <% end %>

  <%= f.primary %>
  <%= link_to "Abbrechen", back_or_default, class: 'btn btn-secondary' %>
<% end %>
