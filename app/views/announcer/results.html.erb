<% content_for :subnav do %>
  <nav class="navbar bg-body-secondary border-bottom py-0 mb-2" style="--bs-nav-link-padding-y: 0.05rem">
    <ul class="navbar-nav flex-row justify-content-around w-100">
      <li class="nav-item">
        <% if @previous_race %>
          <%= link_to announcer_results_path(@regatta, @previous_race.event.number, @previous_race.number, reverse_lanes: params[:reverse_lanes], lane_view: params[:lane_view]), class: 'nav-link prev-button' do %>
            &laquo; <span class="d-none d-lg-inline"><%= @previous_race.full_name %></span><span class="d-lg-none"><%= @previous_race.compact_name %></span>
          <% end %>
        <% end %>
      </li>
      <li class="nav-item">
        <% if @current_race && @current_race.full_name != @race.full_name %>
          <%= link_to announcer_results_path(@regatta, @current_race.event.number, @current_race.number, reverse_lanes: params[:reverse_lanes], lane_view: params[:lane_view]), class: 'nav-link fw-bold current-race-button' do %>
            <span class="d-none d-lg-inline">Aktuell <%= @current_race.full_name %></span><span class="d-lg-none">&#9654; <%= @current_race.compact_name %></span>
          <% end %>
        <% end %>
      </li>
      <li class="nav-item">
        <% if @next_race %>
          <%= link_to announcer_results_path(@regatta, @next_race.event.number, @next_race.number, reverse_lanes: params[:reverse_lanes], lane_view: params[:lane_view]), class: 'nav-link next-button' do %>
            <span class="d-none d-lg-inline"><%= @next_race.full_name %></span><span class="d-lg-none"><%= @next_race.compact_name %></span> &raquo;
          <% end %>
        <% end %>
      </li>
      <li class="nav-item">
        <%= link_to announcer_path(@regatta, reverse_lanes: params[:reverse_lanes], lane_view: params[:lane_view]), class: 'nav-link' do %>
          <span class="d-none d-lg-inline">Zum </span>Dashboard
        <% end %>
      </li>
    </ul>
  </nav>
<% end %>

<turbo-frame id="results">
  <div class="results-with-lane<%= ' results-by-lane' if params[:lane_view].present? %>" style="break-inside: avoid">
    <div class="row align-items-center mb-2">
      <div class="col-12 col-lg">
        <h3 class="mb-0"><%= @race.full_name(show_event_name_short: true) %></h3>
      </div>
      <div class="col-12 col-lg-auto">
        <div class="btn-group" role="group">
          <%= link_to "Nach Zeit", url_for(lane_view: nil, reverse_lanes: nil),
                      class: "btn btn-sm #{params[:lane_view].blank? ? 'btn-outline-success' : 'btn-outline-secondary'}" %>
          <%= link_to "Nach Bahn (Bahn 1 oben)", url_for(lane_view: '1', reverse_lanes: nil),
                      class: "btn btn-sm #{params[:lane_view].present? && params[:reverse_lanes].blank? ? 'btn-outline-success' : 'btn-outline-secondary'}" %>
          <%= link_to "Nach Bahn (Bahn #{@regatta.number_of_lanes} oben)", url_for(lane_view: '1', reverse_lanes: '1'),
                      class: "btn btn-sm #{params[:lane_view].present? && params[:reverse_lanes].present? ? 'btn-outline-success' : 'btn-outline-secondary'}" %>
        </div>
      </div>
    </div>

    <div class="result-row result-row__header">
      <div class="rank">Pos.</div>
      <div class="lane">Bahn</div>
      <div class="number">Bugnr</div>
      <div class="country">Land</div>
      <div class="team">Teamname</div>
      <% if @measuring_points %>
        <div class="times">
          <div class="times-label">Zeiten</div>
          <% @measuring_points.each do |mp| %>
            <div class="time"><%= mp.name %></div>
          <% end %>
        </div>
      <% else %>
        <div class="state">Status</div>
      <% end %>
    </div>

    <% @result_entries
         .then { |r| params[:lane_view].present? ? r.sort_by { |e| e[:lane].presence || e[:participant]&.number.to_i + 1000 } : r }
         .then { |r| params[:reverse_lanes].present? ? r.reverse : r }
         .each do |e| %>
      <div class="result-row result-row--rank-<%= e[:rank] %>"
           style="--rank-indent: <%= [(e[:rank] || 1) - 1, 0].max * 1.5 %>em">
        <div class="rank"><%= e[:rank] %> <%= medal_for(e[:rank]) if e[:rank] %></div>
        <div class="lane"><%= e[:lane] %></div>
        <div class="number"><%= e[:participant]&.number %></div>
        <div class="country"><%= e[:participant]&.team&.country %></div>
        <div class="team"><%= e[:participant]&.team_name(regatta: @regatta) %></div>
        <div class="rower-names">
          <%= e[:participant]&.rower_names(rower_link: -> (r, name) { link_to(name, rower_path(@regatta, r), target: 'announcer-rower') }) %>
        </div>
        <% if @measuring_points %>
          <div class="times">
            <% @measuring_points.each do |mp| %>
              <div class="time">
                <%= e[:result].time_for(mp)&.time %>
                <% if mp.number == @max_measuring_point_number %>
                  <br><%= e[:diff_to_prev] %>
                <% end %>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="state"><%= e[:participant]&.state %></div>
        <% end %>
      </div>
    <% end %>

    <%= render 'regatta/result_footer', race: @race %>
  </div>
  <script>autoreloadAfter(3000)</script>
</turbo-frame>

<script>
  if (!window.perpKeyboardListenerInstalled) {
    window.perpKeyboardListenerInstalled = true
    window.addEventListener("keydown", function (event) {
      if (event.defaultPrevented) {
        return;
      }
      var b = null;
      if (event.key == "ArrowLeft") {
        b = document.querySelector('.prev-button')
      } else if (event.key == "ArrowRight") {
        b = document.querySelector('.next-button')
      } else if (event.key == "Home" || event.key == "F1") {
        b = document.querySelector('.current-race-button')
      }
      if (b) {
        b.click();
        event.preventDefault();
      }
    })
  }
</script>
